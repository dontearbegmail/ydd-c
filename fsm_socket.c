#include "fsm_socket.h"

enum fsms_signals fsms_w_create(struct sfd_dcl_storage *dcl)
{
    /*if(dcl == NULL)
	return fsms_s_err;
    if(fsms == NULL)
	return fsms_s_err;
    if(fsms->ai == NULL)
	return fsms_s_err;
*/


    return fsms_s_noerr;
}
